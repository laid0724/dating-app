import { SpacedCommaPipe } from './spaced-comma.pipe';

describe('SpacedCommaPipe', () => {
  it('create an instance', () => {
    const pipe = new SpacedCommaPipe();
    expect(pipe).toBeTruthy();
  });
});
